export const DEFAULT_EXERCISE: { name: string; duration: number; rest: number } = { name: '', duration: 30, rest: 15 };

export const DEFAULT_CIRCUIT: { rounds: number; warmup: number; cooldown: number; roundRest: number } = {
  rounds: 3,
  warmup: 10,
  cooldown: 20,
  roundRest: 30,
};
